#!/usr/bin/env node
import path from 'node:path';import {normalizedState,saveState,uid,argMap,workspaceFingerprint,root,cdir,readJson} from './lib/core.mjs';
const a=argMap(),cmd=a._[0]||'list',s=normalizedState(),r=root(),reg=readJson(path.join(cdir(r),'agents','registry.json'),{}).agents||[];
if(cmd==='record'){const producer=a.producer,result=String(a.result||'').toUpperCase(),role=reg.find(x=>x.name===producer);if(!role||role.mode!=='read'){console.error('review producer must be a registered read-only role');process.exit(65)}if(!['PASS','FAIL','BLOCKED'].includes(result)){console.error('--result PASS|FAIL|BLOCKED required');process.exit(64)}s.reviews.push({id:a.id||uid('REV'),producer,result,scope:a.scope||'mission',summary:a.summary||null,workspaceFingerprint:workspaceFingerprint(r).fingerprint,timestamp:new Date().toISOString()});saveState(s)}else if(cmd!=='list'){console.error('Usage: review.mjs list|record');process.exit(64)}console.log(JSON.stringify(cmd==='list'?s.reviews:s.reviews.at(-1),null,2));
