#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import {root,cdir,argMap,normalizedState,readJson,workspaceFingerprint} from './lib/core.mjs';
const a=argMap(),r=root(),c=cdir(r),agent=a.agent;if(!agent){console.error('--agent required');process.exit(64)}const reg=readJson(path.join(c,'agents','registry.json'),{}),role=reg.agents?.find(x=>x.name===agent);if(!role){console.error('unknown agent');process.exit(65)}const files=String(a.files||'').split(',').filter(Boolean).map(rel=>({path:rel,exists:fs.existsSync(path.resolve(r,rel))}));const packet={schemaVersion:1,generatedAt:new Date().toISOString(),agent:role,workspace:workspaceFingerprint(r),profile:readJson(path.join(c,'project-profile.json'),null),state:normalizedState(r),files,constraints:{authority:normalizedState(r).mission?.authority||'A3',writerMode:role.mode,localhostContinuity:true}};console.log(JSON.stringify(packet,null,2));
