# Git safety

Preserve unrelated pre-existing dirty work. Inspect status/diff before and after changes. Do not reset, clean, force push, rewrite history, delete branches or discard user work without explicit authorization. Final audit must distinguish task changes from pre-existing changes.
