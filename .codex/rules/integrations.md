# Integrations

Treat external APIs as failure-prone and untrusted. Validate signatures/authentication, retries, timeouts, idempotency, partial failure and reconciliation. External writes are side effects and must follow authority policy.
