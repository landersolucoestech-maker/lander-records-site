# Deterministic control plane

For non-trivial missions, lifecycle state, evidence, independent reviews, findings, side effects, ownership and completion are machine-readable records. Agent prose is advisory; deterministic runtime records are authoritative.

A PASS followed by a newer fresh FAIL is a failure. Runtime state files are excluded from workspace identity so recording governance evidence cannot invalidate itself. Product code, repository configuration, policies and governance source files remain fingerprinted.
