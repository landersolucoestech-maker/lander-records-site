# Database

Schema/data changes require forward/backward compatibility analysis, rollback/recovery strategy and data-integrity evidence. Destructive migrations are L5 and require explicit authority. Never assume application validation replaces database constraints or isolation controls.
