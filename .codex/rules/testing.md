# Testing

Discover repository-native commands before inventing new ones. Prefer the narrowest reliable checks first, then broader regression proportional to impact. Record actual exit status. Never claim a check ran when it did not. L3+ normally requires integration/regression coverage; L5 requires negative-path and security-relevant validation.
