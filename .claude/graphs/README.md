# Graphs

`*.flow.json` are curated semantic flows anchored to code (`path#symbol`); `runtime/graph.mjs check` fails when an anchor disappears. The other JSON files are generated by `runtime/graph.mjs build` from the real source tree (dependencies, symbols, routes, data flows, integration touchpoints, test coverage).
