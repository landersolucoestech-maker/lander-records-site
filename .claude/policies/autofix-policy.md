# autofix-policy

## Policy
Autofix when root cause proven, expected behavior defined, risk acceptable, reversible, validatable. Otherwise park with reason.

## Enforcement
finding.autofix.eligible + reason.
