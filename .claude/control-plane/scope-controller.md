# Scope controller

Implements the literal task; adjacent debt becomes a finding, not an edit. Writers own disjoint paths (agent 'owns' lists). Unexpected diff outside the cluster blocks the commit until explained.
