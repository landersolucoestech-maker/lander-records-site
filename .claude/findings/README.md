# Findings

One JSON file per finding (`F-NNNN.json`, contract `contracts/finding.schema.json`). Write only through `runtime/findings.mjs` (transitions are validated) or auditors following the contract. `state/findings.yml` is the derived index.
