# Evidence

`EV-NNNN.json` records created **only** by `runtime/evidence.mjs` (executed command with real exit code, output hash and excerpt, commit and workspace fingerprint) or `evidence.mjs review` (independent reviewer report containing its VERDICT line). Hand-written evidence is forbidden (evidence-first policy). Evidence is fresh while its fingerprint equals the current workspace.
