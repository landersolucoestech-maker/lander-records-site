# Product schemas

Shapes of real data in lander-records-site (HTTP payloads, outbox envelope, cache rows), derived from the code paths cited in each title. Pack record contracts live in `../contracts/`.
