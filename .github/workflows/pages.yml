name: Legacy GitHub Pages Freeze

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This repository is migrating from static GitHub Pages to a dynamic Next.js CMS runtime."
          echo "The last known-good GitHub Pages artifact remains the public fallback until the dynamic cutover is completed."
